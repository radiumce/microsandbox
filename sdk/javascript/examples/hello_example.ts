/**
 * Simple example showing how to use the microsandbox TypeScript package.
 *
 * Before running this example:
 *   1. Install the package: npm install microsandbox
 *   2. Compile the TypeScript: tsc hello_example.ts
 *   3. Run the compiled JavaScript: node hello_example.js
 */

// Import the microsandbox package
import { greet } from "microsandbox";

// Call the greet function
greet("World");
