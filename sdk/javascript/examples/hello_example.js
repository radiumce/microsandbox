/**
 * Simple example showing how to use the microsandbox JavaScript package.
 *
 * Before running this example:
 *   1. Install the package: npm install microsandbox
 *   2. Run this script: node hello_example.js
 */

// Import the microsandbox package
const { greet } = require("microsandbox");

// Call the greet function
greet("World");
