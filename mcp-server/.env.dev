# Development Environment Configuration
# This file is automatically loaded by start_mcp_dev.sh
# It overrides the default values from .env for development use

# =============================================================================
# MCP Server Configuration - Development Settings
# =============================================================================

# Enable CORS for development (default: false, but useful for dev)
MCP_ENABLE_CORS=true

# config port to avoid conflicts (default: 8775)
MCP_SERVER_PORT=8775

# =============================================================================
# Microsandbox Connection
# =============================================================================

MSB_SERVER_URL=http://127.0.0.1:5555

# =============================================================================
# Custom Python Image
# =============================================================================

# Use our custom Python 3.12 image instead of the default
MICROSANDBOX_PYTHON_IMAGE=microsandbox/python-local:latest

# =============================================================================
# Session Management - Development Friendly
# =============================================================================

MSB_MAX_SESSIONS=5
MSB_SESSION_TIMEOUT=1800
MSB_DEFAULT_FLAVOR=small
MSB_SESSION_CLEANUP_INTERVAL=60

# =============================================================================
# Resource Limits - Conservative for Development
# =============================================================================

MSB_MAX_TOTAL_MEMORY_MB=4096
MSB_MAX_EXECUTION_TIME=1800

# Execution timeout for individual commands (default: 300 seconds)
MSB_EXECUTION_TIMEOUT=1800

# =============================================================================
# Volume Mapping - Development Directories
# =============================================================================

MSB_ENABLE_VOLUME_MAPPING=true
MSB_SHARED_VOLUME_PATH='["/Users/CE/Documents/agent-wd:/sandbox/shared"]'

# =============================================================================
# Logging - Verbose for Development
# =============================================================================

MSB_LOG_LEVEL=DEBUG
MSB_LOG_FORMAT=text