# Core dependencies for MCP Server
aiohttp>=3.8.0
asyncio-mqtt>=0.11.0
pydantic>=1.10.0

# MicroSandbox SDK (assuming it's available via pip or local install)
# microsandbox>=0.1.0

# Testing dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-mock>=3.10.0

# Logging and monitoring
structlog>=22.0.0

# Development dependencies
black>=22.0.0
flake8>=5.0.0
mypy>=1.0.0